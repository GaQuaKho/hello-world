git init ~ Đầu tiên, sử dụng git, tạo kho
git status ~ Hiển thị trạng thái các files

git add . || $ten-flile.duoi // ~ Đưa file từ thư mục làm việc sang chế độ staging
git reset file

git commit -m "$message" ~ Git commit dùng để đưa file ở staging sang commited
git push ~ Đưa 1 commit lên git

git log ~ Xem lại commit đã tạo, nó hiển thị ra $message tạo ở git commit

git branch ~ Xem các branch
git checkout -b $nameBrand ~ Tạo brand và vào branch
git brand -D file
git checkout $nameBrand ~ Chuyển sang band khác
git merge $nameBranch ~ Chuyển file từ branch khac sang branch hien tai
@ : git pull  ~ File trên git và local kahcs thì

git remote add origin link
git remote -v
git push -u origin master
.gitignore